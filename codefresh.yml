version: '1.0'

steps:
  step_1:
    title: Unit Tests under Chrome
    type: composition
    description: Test under Standalone Chrome Selenium
    composition:
      version: '2'
      services:
        selenium:
          image: selenium/standalone-chrome:2.46.0
          ports:
            - 4444:4444
    composition_candidates:
      test:
        image: maven:3.5.2-jdk-8-alpine
#        volumes:
#          - /dev/shm:/dev/shm
        commands:
          - echo "Test"